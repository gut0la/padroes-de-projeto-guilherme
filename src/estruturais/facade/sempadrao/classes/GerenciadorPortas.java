package estruturais.facade.sempadrao.classes;

public class GerenciadorPortas {
    public void criarPorta(String tipo, int sala1, int sala2) {
        System.out.println("Criando porta " + tipo + " entre salas " + sala1 + " e " + sala2);
    }
}
